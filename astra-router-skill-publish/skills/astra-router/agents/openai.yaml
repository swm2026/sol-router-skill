interface:
  display_name: "Astra Router"
  short_description: "Direct execution with on-demand Sol/Astra reasoning"
  default_prompt: "Use $astra-router: execute ordinary work directly with Terra/Luna and involve Sol or Astra only for a concrete planning or decision need."
policy:
  allow_implicit_invocation: false
